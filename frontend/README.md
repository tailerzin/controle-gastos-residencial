# Frontend

Interface web do Controle de Gastos Residencial, feita com React, TypeScript e Vite.

## Comandos

```bash
npm install
npm run dev
npm run build
```

## API

A URL base da API fica em `src/api/api.ts`.
